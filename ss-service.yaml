apiVersion: v1
kind: Service
metadata:
  name: ss-service
  labels:
    app: ss-devops-project
spec:
  selector:
    app: ss-devops-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200