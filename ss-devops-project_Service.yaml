apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: ss-deployment
spec:
  selector:
    matchLabels:
      app: ss-devops-project
  replicas: 2 # tells deployment to run 2 pods matching the template
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1

  template:
    metadata:
      labels:
        app: ss-devops-project
    spec:
      containers:
      - name: ss-devops-project
        image: mhali/java_app
        imagePullPolicy: Always
        ports:
        - containerPort: 8080

#For Service
apiVersion: v1
kind: Service
metadata:
  name: ss-service
  labels:
    app: ss-devops-project
spec:
  selector:
    app: ss-devops-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200